package wheatsdfasdf;

public class fsdafasdfasdf {
	
	/*
	 * 
	 * Instances finalfile
	 * splitRecord = String.split
	double[] values = new double[5];
	
	
	values[1] = FinalFile.attribute(1).addStringValue(splitRecord[1]);
	values[2] = FinalFile.attribute(2).addStringValue(splitRecord[2]);
	
	values[4] = FinalFile.attribute(4).parseDate(splitRecord[4]);
	*/
}
